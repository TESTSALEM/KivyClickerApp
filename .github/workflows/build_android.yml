name: Kivy Android Build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install System Dependencies 🏗️
      # تثبيت متطلبات نظام Linux الأساسية لـ P4A
      run: |
        sudo apt update
        sudo apt install -y git zip unzip openjdk-17-jdk python3-pip autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev libblas-dev liblapack-dev libssl-dev build-essential lld

    - name: Prepare Python Environment
      run: |
        # التأكد من تحديث أدوات بناء Python
        pip install --upgrade pip setuptools wheel

    - name: Install Buildozer and Cython
      run: |
        pip install --upgrade buildozer cython 

    - name: Initialize and Accept SDK Licenses 🔑
      # تحديث المكونات وقبول التراخيص (لحل مشكلة Aidl والترخيص)
      run: |
        buildozer android update

    - name: Final Build Kivy App 🚀
      # بدء البناء الفعلي
      run: |
        buildozer android debug

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: Android-APK-Release
        path: bin/*.apk
